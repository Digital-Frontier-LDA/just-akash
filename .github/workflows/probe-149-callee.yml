# ⚠ PROBE — df-cicd#149. TEMPORARY; delete with this branch.
name: Probe 149 callee
on:
  workflow_call:
permissions:
  contents: read
jobs:
  noop:
    name: noop
    runs-on: ubuntu-latest
    steps:
      - run: echo "arm C resolved — reusable machinery works in this repo"
